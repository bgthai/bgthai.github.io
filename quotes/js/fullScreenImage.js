function showFullSizeImage(pictureElement){const imgElement=pictureElement.querySelector("img");if(!imgElement)return;const currentSrc=imgElement.currentSrc||imgElement.src;if(currentSrc.endsWith("avif")){const altText=imgElement.alt;const overlay=document.createElement("div");overlay.className="image-overlay";function closeOverlay(){document.body.removeChild(overlay);document.removeEventListener("keydown",handleEscKey);}
const imgContainer=document.createElement("div");imgContainer.className="image-container";const img=document.createElement("img");img.src=currentSrc;img.alt=altText;img.style.maxWidth="90vw";img.style.maxHeight="90vh";const closeButton=document.createElement("span");closeButton.className="close-button";closeButton.innerHTML="&times;";closeButton.onclick=closeOverlay;function handleEscKey(event){if(event.key==="Escape"){closeOverlay();}}
document.addEventListener("keydown",handleEscKey);overlay.onclick=closeOverlay;imgContainer.onclick=(event)=>event.stopPropagation();imgContainer.appendChild(img);imgContainer.appendChild(closeButton);overlay.appendChild(imgContainer);document.body.appendChild(overlay);}}
document.addEventListener("DOMContentLoaded",function(){document.querySelectorAll("picture").forEach(picture=>{const img=picture.querySelector("img");const avifSource=picture.querySelector("source[type='image/avif']");const googleId=img.getAttribute("data-google-id");const googleDriveLink=`https://drive.google.com/file/d/${googleId}/view`;const testImg=new Image();testImg.src=avifSource.getAttribute("srcset");testImg.onload=function(){};testImg.onerror=function(){const link=document.createElement("a");link.classList.add("galleryImage")
link.href=googleDriveLink;link.target="_blank";link.appendChild(img.cloneNode(true));picture.innerHTML="";picture.appendChild(link);};});});